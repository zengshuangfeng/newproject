<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.meisui.manage.dao.Iuser_deviceDao">
    <select id="getIdByUid" resultType="Long">
         select id from t_user_device where uid = #{uid} limit 1
    </select>
</mapper>